import { DocumentTextFilterPipe } from './document-text-filter.pipe';

describe('DocumentTextFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentTextFilterPipe();
    expect(pipe).toBeTruthy();
  });
});