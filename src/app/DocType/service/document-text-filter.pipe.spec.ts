import { DocumentTypeTextFilterPipe } from './document-text-filter.pipe';

describe('DocumentTypeTextFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentTypeTextFilterPipe();
    expect(pipe).toBeTruthy();
  });
});